Do not make a code change without getting approval of the user first.

Prefer building scripted logic in Python or other languages rather than building instructions into command.md, which is an Anthropic only convention.

